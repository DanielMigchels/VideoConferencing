name: Build Application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  DOTNET_VERSION: '9.0.x'
  NODE_VERSION: '24.x'

jobs:

  Build:
    name: Build Application
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: './VideoConferencing.UI/package-lock.json'

    - name: Install Angular dependencies
      working-directory: ./VideoConferencing.UI
      run: npm install

    - name: Test Angular
      working-directory: ./VideoConferencing.UI
      run: npm run test -- --watch=false --browsers=ChromeHeadless || echo "No tests configured"

    - name: Build Angular (Production)
      working-directory: ./VideoConferencing.UI
      run: npm run build -- --configuration production

    - name: Copy Angular build to API wwwroot
      run: |
        mkdir -p ./VideoConferencing.API/VideoConferencing.API/wwwroot/
        rm -rf ./VideoConferencing.API/VideoConferencing.API/wwwroot/*
        cp -r ./VideoConferencing.UI/dist/VideoConferencing.UI/browser/* ./VideoConferencing.API/VideoConferencing.API/wwwroot/ || cp -r ./VideoConferencing.UI/dist/* ./VideoConferencing.API/VideoConferencing.API/wwwroot/

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore .NET dependencies
      working-directory: ./VideoConferencing.API
      run: dotnet restore

    - name: Build .NET API
      working-directory: ./VideoConferencing.API
      run: dotnet build --configuration Release --no-restore

    - name: Test .NET API
      working-directory: ./VideoConferencing.API
      run: dotnet test --configuration Release --no-build --verbosity normal --collect:"XPlat Code Coverage"

    - name: Publish .NET API as executable (Windows)
      working-directory: ./VideoConferencing.API
      run: |
        dotnet publish VideoConferencing.API/VideoConferencing.API.csproj \
          --configuration Release \
          --runtime win-x64 \
          --self-contained true \
          --output ./publish/win-x64 \
          -p:PublishSingleFile=true \
          -p:IncludeNativeLibrariesForSelfExtract=true

    - name: Publish .NET API as executable (Linux)
      working-directory: ./VideoConferencing.API
      run: |
        dotnet publish VideoConferencing.API/VideoConferencing.API.csproj \
          --configuration Release \
          --runtime linux-x64 \
          --self-contained true \
          --output ./publish/linux-x64 \
          -p:PublishSingleFile=true \
          -p:IncludeNativeLibrariesForSelfExtract=true

    - name: Upload Windows artifacts
      uses: actions/upload-artifact@v4
      with:
        name: videoconferencing-windows-x64
        path: ./VideoConferencing.API/publish/win-x64
        retention-days: 7

    - name: Upload Linux artifacts
      uses: actions/upload-artifact@v4
      with:
        name: videoconferencing-linux-x64
        path: ./VideoConferencing.API/publish/linux-x64
        retention-days: 7